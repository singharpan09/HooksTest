//here will use "useref" hook in functional component

import React, { useref } from "react";

const Ref = () => {
  return (
    <React.Fragment>
      <h3>this is ref Component</h3>
    </React.Fragment>
  );
};

export default Ref;
